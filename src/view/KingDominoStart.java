package view;

import model.set.NumberPlayerStrategy;
import model.set.number.*;

public class KingDominoStart {

    //private final NumberPlayerStrategy[] strategyPlayers;

    public KingDominoStart()
    {
        //On ajoute dans la liste des Strategies :
        //strategyPlayers = new NumberPlayerStrategy[]{new Duo(), new Trio(), new Quatro()};
    }
}
